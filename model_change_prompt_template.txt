

На основе описанных изменений САМОСТОЯТЕЛЬНО выполни во всём проекте следующее:

1) МОДЕЛИ
- Обнови все затронутые модели в `core/models.py` и других файлах моделей (если есть).
- При необходимости приведи в порядок `related_name`, `verbose_name`, `help_text`.

2) МИГРАЦИИ
- Создай/обнови Django‑миграции для этих изменений.
- Если есть перенос данных (data migration) — аккуратно перенеси существующие данные между полями/таблицами.

3) СЕРИАЛИЗАТОРЫ
- Обнови все связанные сериализаторы в `api/serializers.py` (и других файлах, если требуется), чтобы они соответствовали новым полям/связям.
- Обнови `fields`, `read_only`, `write_only`, `help_text` и т.п.

4) API (views + urls + документация)
- Обнови API‑вьюхи (например, `api/views.py`), чтобы они корректно работали с новыми полями/связями.
- При необходимости обнови `select_related` / `prefetch_related`.
- Обнови URL‑маршруты (если нужно).
- Обнови описания/схемы для Swagger/DRF Spectacular, если они завязаны на эти поля.

5) ТЕСТЫ API
- Обнови/дополни тесты в `api/tests.py`, чтобы они:
  - проверяли наличие/отсутствие новых/старых полей;
  - учитывали новые связи и валидацию;
  - при необходимости создавали корректные тестовые объекты.

6) ИНТЕРФЕЙС @admin_panel
- Обнови формы в `admin_panel/forms.py` (списки `fields`, виджеты и т.п.).
- Обнови вьюхи в `admin_panel/views.py`, если затрагиваются queryset’ы или логика отображения.
- Обнови шаблоны в `templates/admin/*` (формы, списки, детали), чтобы новые поля:
  - отображались в списках и карточках;
  - были доступны для редактирования.

7) ШТАТНАЯ АДМИНКА Django (@admin)
- Обнови настройки в `core/admin.py`:
  - `list_display`, `list_filter`, `search_fields`;
  - `fieldsets`, `readonly_fields`, `raw_id_fields`, `filter_horizontal` и т.п.
- Убедись, что новые поля корректно редактируются в стандартной админке.

8) ТЕСТОВЫЕ ДАННЫЕ И УТИЛИТЫ
- Обнови команду `core/management/commands/create_dummy_data.py` (и другие утилиты), чтобы они создавали валидные объекты с учётом новых полей/связей.
- При необходимости обнови фикстуры, фабрики, сидеры (если есть).

9) ДОКУМЕНТАЦИЯ
- Обнови `docs/domain-entities.md`, `README.md` и другие документы, где описываются сущности и их поля, чтобы описание соответствовало новым моделям.

10) КАЧЕСТВО КОДА
- Проверь изменения на предмет очевидных ошибок и несоответствий.
- Если возможно, укажи, какие команды мне выполнить локально (например: `python manage.py makemigrations`, `python manage.py migrate`, `pytest`), но САМ их не запускай, только перечисли.

Общие требования:
- Если чего‑то не хватает в моём описании, делай разумные предположения и явно пропиши их словами.
- Сохраняй существующий стиль кода, нейминг и структуру проекта.
- В ответе показывай только ключевые фрагменты кода и объяснения — без избыточных простынь.
